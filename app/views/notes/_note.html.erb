<div class="border-b py-2 last:border-b-0" id="<%= dom_id(note) %>">
  <p class="text-sm"><%= note.body %></p>
  <p class="text-xs text-gray-400 mt-1"><%= note.author %> • <%= time_ago_in_words(note.created_at) %> ago</p>
</div>
